<div class="popup-position popup-wrapper" id="remove-clients-modal">
    <div id="popup-container">
        <h2>Remover cliente</h2>
        <p> Você está prestes a remover o cliente de ID [<label id="remove-clients-modal-id-label"></label>] </p>
        <p> Deseja continuar? </p>
        <button class="pretty-button" onclick="toggleRemoveModal(0)">Cancelar</button>
        <a href="#" id="remove-clients-modal-reference">
            <button class="pretty-button button-red">Remover</button>
        </a>
    </div>

    <script>
        function toggleRemoveModal(userid) {
            if (userid) {
                document.getElementById('remove-clients-modal-id-label').textContent = userid;
                document.getElementById('remove-clients-modal-reference').href="remove/client?id="+userid; 
            }
            document.getElementById('remove-clients-modal').classList.toggle("modal-show");
        }
    </script>
</div>